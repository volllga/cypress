npm install cypress --save-dev  // parameter adds cypress to the package.json of the project

npx cypress open

/support/index.js     // very first file when an execution starts
/support/commands.js  // common functions
